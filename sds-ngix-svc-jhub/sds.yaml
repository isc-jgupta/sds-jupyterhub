apiVersion: v1

compute:
  computerSpecs:
  - name: hub
    label: Main Jupyter
    nodeSelectorField: hub.nodeSelector 
    cpuRequestField: hub.cpu
    memoryRequestField: hub.memory
  - name: proxy
    label: Proxy Pod for JHub
    nodeSelectorField: proxy.chp.nodeSelector 
    cpuRequestField: proxy.cpu
    memoryRequestField: proxy.memory
  - name: hub
    label: Main Jupyter
    nodeSelectorField: hub.nodeSelector 
    cpuRequestField: hub.cpu
    memoryRequestField: hub.memory

